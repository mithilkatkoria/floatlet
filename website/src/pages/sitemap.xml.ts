import {product} from '../data/site';import pages from '../data/pages.json';export function GET(){const paths=product.mode==='production'&&product.origin?['/','/download/','/dynamic-island-for-windows/',...pages.filter(p=>!['privacy','terms'].includes(p.slug)).map(p=>'/'+p.slug+'/')]:[];return new Response('<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">'+paths.map(p=>'<url><loc>'+new URL(p,product.origin).href+'</loc></url>').join('')+'</urlset>',{headers:{'Content-Type':'application/xml; charset=utf-8'}})}